# Межгалактическая Аналитика

React-приложение для анализа CSV файлов с данными о расходах различных цивилизаций в галактических кредитах.

## 🚀 Инструкции по запуску

### Установка и запуск

1. **Клонирование репозитория**
   ```bash
   git clone https://github.com/TimurZheksimbaev/Yandex-SHRI-React-App.git
   cd Yandex-SHRI-React-App
   ```

2. **Установка зависимостей фронтенда**
   ```bash
   npm install
   ```

3. **Откройте новую вкладку терминала. Клонирование и Установка зависимостей бэкенда**
   ```bash
   git clone https://github.com/etozhenerk/shri2025-back.git
   cd shri2025-back
   npm install
   ```

4. **Запуск бэкенда**
   ```bash
   npm start
   ```
   Бэкенд будет на `http://localhost:3000`

5. **Запуск фронтенда**
   ```bash
   npm run dev
   ```
   Фронтенд будет доступен на `http://localhost:5173`

### Доступные скрипты

**Фронтенд:**
- `npm run dev` - запуск в режиме разработки
- `npm run build` - сборка для продакшена
- `npm run preview` - предварительный просмотр сборки
- `npm run lint` - проверка кода линтером
- `npm run test` - запуск модульных тестов
- `npm run test:coverage` - запуск coverage тестов
- `npm run test:e2e` - запуск e2e тестов

**Бэкенд:**
- `npm run start` - запуск бэкенда

### Непрерывная интеграция (CI)

В проекте настроен `CI/CD` процесс с тестами
- Модульные тесты с Vitest
- coverage тесты с vitest coverage

Результаты тестов и отчеты о покрытии доступны в разделе Actions в репозитории.

## 🏗️ Архитектура

### Общая структура

Проект состоит из двух основных частей:
- **Frontend** (React + TypeScript + Vite)
- **Backend** (Node.js + Fastify)

#### Технологический стек Frontend

- **React 19**
- **TypeScript**
- **Vite**
- **CSS Modules**
- **ESLint**

#### Основные компоненты

1. **Header** - навигационная панель с логотипом и меню
2. **Home** - основная логика приложения, управление состоянием
3. **DropZone** - drag & drop загрузка файлов с различными состояниями
4. **AnalyticsResults** - отображение результатов в виде карточек
5. **LoadingDisplay** - индикатор загрузки с прогрессом
6. **CompletedDisplay** - состояние успешного завершения

#### Хуки

- **useDragAndDrop** - обработка drag & drop функциональности

### Backend

#### API Endpoints

1. **POST /aggregate** 
   - Обработка CSV файлов

2. **GET /report**
   - Генерация тестовых CSV файлов

### Интеграция Frontend-Backend

#### API Service `src/services/api.ts`

- **ApiService.aggregateFile()** - отправка файла и получение результатов
- **ApiService.generateReport()** - генерация тестовых данных

# Автоматизированное тестирование сервиса межгалактической аналитики

## Реализация тестов

Для автоматизированного тестирования приложения были использованы следующие инструменты:

- **Vitest** - для модульного и интеграционного тестирования
- **React Testing Library** - для тестирования компонентов
- **Playwright** - для E2E тестирования

## Описание тестовых сценариев

### 1. Загрузка таблиц с данными и получение аналитики
- **Основные сценарии:**
  - Загрузка валидного CSV файла через drag&drop
  - Загрузка валидного CSV файла через кнопку
  - Отправка файла на обработку и получение результатов
  - Очистка интерфейса после загрузки
  
- **Граничные случаи:**
  - Загрузка файла неверного формата
  - Загрузка пустого CSV файла
  - Обработка очень большого CSV файла
  
- **Обработка ошибок:**
  - Обработка ошибки сервера при отправке файла
  - Обработка ошибки сети при отправке файла

### 2. Отображение прогресса обработки
- **Основные сценарии:**
  - Отображение индикатора загрузки при отправке файла
  - Постепенное обновление результатов в процессе обработки
  - Отображение финального результата после завершения обработки
  
- **Граничные случаи:**
  - Проверка троттлинга обновлений UI (не чаще чем раз в 300мс)
  - Тестирование с очень медленным соединением
  
- **Обработка ошибок:**
  - Прерывание загрузки в процессе обработки

### 3. Генератор тестовых данных
- **Основные сценарии:**
  - Генерация файла по умолчанию
  - Автоматическая загрузка файла после генерации
  - Добавление записи в историю после генерации
  
- **Граничные случаи:**
  - Генерация файла с ошибками
  - Генерация файла максимального размера
  
- **Обработка ошибок:**
  - Обработка ошибки сервера при генерации
  - Обработка ошибки сети при генерации

### 4. История загрузок
- **Основные сценарии:**
  - Загрузка истории из LocalStorage при открытии страницы
  - Добавление новой записи в историю после загрузки файла
  - Добавление новой записи в историю после генерации файла
  - Удаление отдельной записи из истории
  - Полная очистка истории
  - Просмотр деталей записи в модальном окне
  
- **Граничные случаи:**
  - Работа с пустой историей
  - Работа с большим количеством записей в истории
  
- **Обработка ошибок:**
  - Обработка некорректных данных в LocalStorage

### 5. Навигационное меню
- **Основные сценарии:**
  - Переход между разделами приложения
  - Корректное отображение активного раздела
  - Переход на страницу генерации со страницы истории
  
- **Граничные случаи:**
  - Навигация по несуществующим маршрутам
  
- **Обработка ошибок:**
  - Обработка ошибок при переходе между страницами

## Используемые технологии

1. **Unit-тесты:**
   - Vitest - для модульного тестирования функций и хуков
   - React Testing Library - для тестирования компонентов

2. **Интеграционные тесты:**
   - React Testing Library - для тестирования взаимодействия компонентов
   - MSW (Mock Service Worker) - для мокирования API-запросов

3. **E2E-тесты:**
   - Playwright - для сквозного тестирования основных пользовательских сценариев

## Запуск

### Установка зависимостей
```bash
npm install
```

### Запуск unit и интеграционных тестов
```bash
npm test
```

### Запуск тестов с отчетом о покрытии
```bash
npm run test:coverage
```

### Запуск приложения
```bash
npm run dev
```

### Запуск бэкенда
```bash
cd shri2025-back
npm install
npm start
```

### Запуск e2e тестов
```bash
npm run test:e2e
```

## Типы тестов

В проекте использованы различные типы тестов для оптимального покрытия функциональности:

1. **Модульные тесты (Unit Tests)**
2. **Интеграционные тесты**
3. **E2E тесты**
